name: GPT Copilot Tasks

on:
  workflow_dispatch:
  push:
    paths:
      - '**.md'
  pull_request:
    paths:
      - '**.md'

jobs:
  gpt_task_runner:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3

      - name: Read task file
        run: |
          echo "===== Task file content ====="
          cat prompts/tasks.md || echo "tasks.md not found"
          echo "=============================="

      - name: Done
        run: echo "✅ Copilot workflow ran successfully."
